package lib

object Servers {
  // model-service
  val modelServer = "localhost:8080"
  // persistence-service
  val persistenceServer = "localhost:8081"
  // core-service
  val coreServer = "localhost:8082"
  // gui-service
  val guiServer = "localhost:8083"
  // tui-service
  val tuiServer = "localhost:8084"
  // mongodb
  val mongoServer = "localhost:27017"
  // postgresdb
  val postgresServer = "localhost:5432"
}
